{
  "reviewType": "implementation",
  "featureName": "earn-roi-reconciler",
  "sprintNumber": 1,
  "iteration": 2,
  "mode": "lean",
  "artifactsToReview": {
    "spec": [".vcsdd/features/earn-roi-reconciler/specs/behavioral-spec.md",
             ".vcsdd/features/earn-roi-reconciler/specs/verification-architecture.md"],
    "source_python": ["skills/_shared/lib/reconciler.py",
                       "skills/_shared/proactive-loop-dispatch.py",
                       "skills/_shared/lib/step6_act.py"],
    "tests": ["skills/_shared/__tests__/test_reconciler.py",
              "skills/_shared/__tests__/test_reconciler_integration.py"]
  },
  "previousIterationFindings": [
    "FIND-001 critical (same-value positive returned noop not skipped_dup): fixed — ternary rewritten; test split into two cases (positive=skipped_dup, both-zero=noop).",
    "FIND-002 critical (malformed roi lines dropped on rewrite): fixed — __raw__ sentinel preserves malformed roi lines verbatim across atomic replace. New test proves lossless round-trip.",
    "FIND-003 high (PROP-R5 crash-safety untested): fixed — monkeypatched os.replace to raise on 1st call; SHA of roi.jsonl before/after asserts unchanged.",
    "FIND-004 high (multiple malformed settle collapse to 1 via (None,None) key): fixed — unique per-line __malformed__:<offset> pass_id + negative ts fallback. Test asserts 3 malformed lines → 3 unmatched entries.",
    "FIND-005 high (PROP-M3 pick_next untested): fixed — 2 new tests (earner wins by ROI score; reconciler wins alone with cadence).",
    "FIND-006 medium (tautological ternary dead code): fixed as byproduct of FIND-001."
  ],
  "reviewDimensions": ["spec_fidelity", "edge_case_coverage", "implementation_correctness", "structural_integrity", "verification_readiness"],
  "extraConstraints": [
    "Verify FIND-002 closure: the __raw__ sentinel is emitted BYTE-IDENTICAL on rewrite (no json.dumps re-serialization).",
    "Verify FIND-003 closure: the crash-safety test injects the failure at the FIRST os.replace call (roi.jsonl), not a later one (offset write).",
    "Verify FIND-004 closure: 3 malformed lines produce 3 unique keys — grep for the __malformed__: pattern.",
    "Verify FIND-005 closure: the earner has score 100 * 0.01 = 1.0 > 0.0, and the parametrized test uses pick_next as-is."
  ],
  "expectedOutputPath": ".vcsdd/features/earn-roi-reconciler/reviews/sprint-1/iteration-2/output/"
}

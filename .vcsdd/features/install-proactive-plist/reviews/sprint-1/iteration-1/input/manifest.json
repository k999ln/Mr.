{
  "reviewType": "implementation",
  "featureName": "install-proactive-plist",
  "sprintNumber": 1,
  "iteration": 1,
  "mode": "lean",
  "artifactsToReview": {
    "spec": [".vcsdd/features/install-proactive-plist/specs/behavioral-spec.md",
             ".vcsdd/features/install-proactive-plist/specs/verification-architecture.md"],
    "source_python": ["skills/_shared/lib/plist_render.py"],
    "source_shell": ["skills/_shared/install-proactive-plist.sh"],
    "tests": ["skills/_shared/__tests__/test_plist_render.py",
              "skills/_shared/__tests__/test_install_integration_darwin.py",
              "skills/_shared/__tests__/test_install_no_human_touch.py"]
  },
  "reviewDimensions": ["spec_fidelity", "edge_case_coverage", "implementation_correctness",
                       "structural_integrity", "verification_readiness"],
  "extraConstraints": [
    "REQ-A4 ordering: validation MUST be the FIRST executable line after arg parse — check the .sh line ordering: validate before any mkdir, file write, or launchctl call.",
    "REQ-E2 collision detect: confirm parse_loaded_plist_path is invoked when launchctl print returns non-empty, and bootout fires only if path != canonical.",
    "REQ-A2 pin: REPO_ROOT computation must yield /Users/operator/anicca (= 2 levels up from skills/_shared) — check the cd .. && cd .. && pwd is correct (could be off-by-one).",
    "FIND-007 single-source: grep .sh for any line that re-defines validate/render/parse/digest in bash — must be 0 hits.",
    "EDGE-E5 rollback: bootstrap failure path must rm the disk plist — check the error branch.",
    "Live E2E succeeded against gig-probe slot (manual run + 264/264 tests). Verify the test suite genuinely exercises the spec, not just trivial happy paths."
  ],
  "expectedOutputPath": ".vcsdd/features/install-proactive-plist/reviews/sprint-1/iteration-1/output/"
}

# 13c-SELL x402 sale ledger implementation plan

**Goal:** Automatically and idempotently book finalized, externally funded x402 USDC sales in the
Rockstar_ibot earnings ledger without classifying self-pay or fractional-cent rounding as revenue.

**Architecture:** Keep the deployed x402 `external-inflows-*.jsonl` files as sale-evidence SSOT.
A small Rockstar_ibot adapter validates their schema, and the production CLI independently queries
Base for chain ID, finalized head, receipt, transaction initiator, and exact USDC Transfer before
calling the existing earnings runtime. A five-minute launchd job performs the connection continuously.

**Tech stack:** Node.js CommonJS, built-in `node:test`, Base JSON-RPC, Supabase REST, launchd.

---

### Task 1: Pure sale-to-ledger mapping

**Files:**
- Create: `apps/rockstar_ibot/lib/x402-sale-ledger.js`
- Create: `apps/rockstar_ibot/lib/x402-sale-ledger.test.js`

1. Write failing tests for a real-shaped exact-cent sale, deterministic key, public evidence, and
   idempotent writer injection.
2. Write failing tests for unknown source, malformed IDs, unowned payTo, mismatched `to`, self payer,
   non-finalized/failed/non-external flags, invalid transaction/address, and fractional-cent amount.
3. Run the test file and record the expected RED caused by the missing module.
4. Implement strict validation and mapping through `usdMinorFromAtomic`.
5. Run the focused tests to GREEN.
6. Commit and push.

### Task 2: Production JSONL and Base re-verification

**Files:**
- Create: `apps/rockstar_ibot/scripts/record-x402-sales.js`
- Create: `apps/rockstar_ibot/scripts/record-x402-sales.test.js`

1. Write failing tests for empty ledgers, malformed lines, successful chain re-verification, self
   transaction initiator, transfer mismatch, sub-cent blocking, and duplicate visibility.
2. Implement bounded JSONL parsing and repeated `--ledger` input.
3. Implement Base mainnet checks: chain `8453`, finalized head, successful receipt at or below the
   finalized head, exactly one USDC Transfer to payTo with exact amount, external initiator.
4. Inject the existing runtime writer after all rows have passed verification.
5. Print only safe counters and public transaction evidence.
6. Run focused adapter and script tests to GREEN.
7. Commit and push.

### Task 3: Continuous production wiring

**Files:**
- Create: `apps/rockstar_ibot/scripts/x402-sale-ledger-boot.sh`
- Create: `apps/rockstar_ibot/launchd/ai.anicca.rockstar_ibot-x402-ledger.plist.template`
- Create: `apps/rockstar_ibot/scripts/install-x402-sale-ledger-launchd.sh`
- Test: `apps/rockstar_ibot/scripts/record-x402-sales.test.js`

1. Add a static contract test for absolute executables, bounded timeout, explicit state paths, and
   non-secret log output.
2. Implement boot and installer scripts using the canonical checkout path substituted at install.
3. Run shell syntax checks and focused Node tests.
4. Install the plist, bootstrap it, then `launchctl kickstart -k` the real loop.
5. Verify a fresh exit-0 log record. Zero source rows is healthy liveness, not a revenue claim.
6. Commit and push.

### Task 4: Production evidence and SSOT

**Files:**
- Create: `docs/evidence/agent-economy/2026-07-27-x402-ledger-bridge.json`
- Modify: `docs/superpowers/specs/2026-07-19-anicca-one-repo-consolidation-spec.md`
- Modify: `docs/handovers/2026-07-27-crypto-track-handoff.md`

1. Read Supabase for `source=x402_sale`; require exact zero or exact expected rows.
2. Record launchd state, fresh loop counters, production row count, and the explicit `$0.00` evidence
   limit without secrets.
3. Mark the 13c-SELL connection ready but keep its cumulative `$1.00` gate pending.
4. Move the executable cursor to 13c-WORK while the SELL loop continues.
5. Run the focused suite and relevant existing earnings suite.
6. Commit, push, open PR, inspect checks, merge only after target verification passes.

# EARN-HC-1 Earning Rail Health Implementation Plan

**Goal:** Every required earning slot and the four portfolio rails (`PM`, `x402`, `WORK`, `CAPITAL`) return one machine-readable state — `operational`, `degraded`, `frozen`, or `not-live` — with fresh evidence. The production registry contains zero `instrumented:false` entries and the live report contains zero `NOT-INSTRUMENTED` results.

**Architecture:** Extend the existing `skills/self/earning-health-registry.json`; do not create a competing registry. Keep the existing trace-barren self-heal path for PM/SOL, add explicit probe contracts for HTTP, heartbeat, funded-account, and disabled rails, and aggregate slot results into the four portfolio rails. Probe output is evidence, not revenue: zero buyers or zero trades does not make a healthy mechanism degraded.

**State contract:** `operational` means the mechanism can accept work now; `degraded` means configured but a required probe is stale/failing; `frozen` means an intentional money-safety stop; `not-live` means deliberately unconfigured/unfunded/unloaded. Unknown or malformed probe results fail closed to `degraded`, never to `operational`.

**Primary-source basis:**

- Kubernetes distinguishes readiness from liveness: “Readiness probes determine when a container is ready to accept traffic.”
  https://kubernetes.io/docs/concepts/configuration/liveness-readiness-startup-probes/
- Kubernetes condition records carry a finite status, probe time, and machine-readable reason: status is `True`, `False`, or `Unknown`; fields include `lastProbeTime` and `reason`.
  https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-conditions

The local four-state vocabulary is the approved Rockstar_ibot SSOT contract; the source above supplies the probe/evidence pattern, not the product names.

---

## Task 1: Pure slot and portfolio classification

**Files:**

- Create: `skills/self/earning-rail-health.mjs`
- Create: `skills/self/tests/earning-rail-health.test.mjs`

- [x] RED: table tests for fresh trace, stale trace, intentional KILL, HTTP 2xx/non-2xx, fresh/stale/missing heartbeat, funded/unfunded account, and explicit disabled slot.
- [x] GREEN: implement only the pure classification needed by those tests.
- [x] RED/GREEN: portfolio aggregation. Any operational member makes the portfolio operational; otherwise degraded outranks frozen, and all inactive members become frozen or not-live without inventing health.
- [x] Require every result to contain `id`, finite state enum, `probedAt`, `reason`, and public evidence fields only.

## Task 2: Upgrade the one existing registry

**Files:**

- Modify: `skills/self/earning-health-registry.json`
- Modify: `skills/self/tests/earning-rail-health.test.mjs`

- [x] Add a probe to all eight existing slots: PM/SOL trace, gig/clip/video heartbeat, x402 HTTP, HL funded-account, token explicit activation.
- [x] Set every production slot to `instrumented:true`; remove all production `gapNote` placeholders.
- [x] Add exactly four portfolio rails with deterministic membership: `PM`, `x402`, `WORK`, `CAPITAL`.
- [x] Test registry closure: false instrumentation count 0, required probe count equals slot count, macro rail ids exact, member references valid.

## Task 3: Effectful CLI and existing health-loop integration

**Files:**

- Modify: `skills/self/earning-rail-health.mjs`
- Modify: `skills/self/earning-health-allslots.sh`
- Modify: `skills/self/tests/test_earning_health_allslots.sh`
- Modify: `skills/self/tests/earning-rail-health.test.mjs`

- [x] CLI resolves paths from `ANICCA_HOME` / `HOME` / script location; no Dais-specific absolute path in shared code.
- [x] HTTP probe uses a bounded timeout; heartbeat/trace reads are bounded; HL account check is read-only; output allowlists fields and contains no key material.
- [x] Existing all-slots loop emits the four-state report first, then retains its existing trace-barren self-fix behavior only for trace probes.
- [x] Legacy test fixtures remain supported; production registry emits no `NOT-INSTRUMENTED`.
- [x] Invalid registry/probe/network input exits nonzero or emits `degraded` with a reason; it never fabricates `operational`.

## Task 4: Live proof, SSOT, and release

**Files:**

- Create: `docs/superpowers/evidence/earn-hc-1-live.json`
- Modify: `docs/superpowers/specs/2026-07-19-anicca-one-repo-consolidation-spec.md`

- [x] Run the live report against the active Franklin instance and production x402 endpoint.
- [x] Require slot states for PM, SOL, gig, x402, HL, token, clip, and video; require portfolio states for PM/x402/WORK/CAPITAL; `NOT-INSTRUMENTED=0`.
- [x] Independently re-read PM trace/KILL, x402 HTTP, gig heartbeat, HL account balance, and loaded/unloaded loop state.
- [x] Run focused tests, existing earning-health tests, full relevant suite, secret scan, JSON parse, and `git diff --check`.
- [x] Update the SSOT with actual states and evidence, advance the cursor to external `$1`, commit/push, merge, and confirm canonical main contains the merge.

# OSS-MERGE-1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the canonical `Daisuke134/life-manager` checkout the only required source tree for the Rockstar_ibot product, with portable install/test/eval commands and no private runtime, local-folder, submodule, or generated-artifact dependency.

**Architecture:** A repository-level verifier is the executable boundary: it reads the Git index, source manifest, active runtime files, installer output, and clean-clone result. Reusable code is copied from pinned source commits into canonical paths with provenance; private state and product-specific personal configuration stay external. Public third-party dependencies use pinned package/source downloads with checksum and license metadata rather than Git submodules.

**Tech Stack:** Git, Node.js 22 test runner, Python 3.11 stdlib, Bash, npm `ci`, GitHub Actions, SHA-256 manifests.

## Global Constraints

- Existing Mac loops remain loaded and unchanged throughout this atomic.
- Canonical repository and push target are `https://github.com/Daisuke134/life-manager`.
- Required Rockstar_ibot code may not resolve through `~/profitable-claude`, `~/anicca`, `~/.openclaw`, `/Users/operator`, a private Git URL, a Git submodule, or a symlink escaping the repository root.
- `anicca-dais` secrets, identity, logs, state, generated media, and credentials remain external inputs and are never copied.
- Anicca/Honne product-specific code and personal product data are not fixed public Rockstar_ibot capabilities.
- New behavior and bug fixes use RED → GREEN → REFACTOR.
- Completion requires a clean clone at the branch commit, `npm ci`, focused tests, full tests, all evals, installer verification, and repository verifier PASS.
- Sources: npm documents `npm ci` as the clean, frozen install for automated environments (`https://docs.npmjs.com/cli/v11/commands/npm-ci`); Git documents submodules as embedded separate repositories (`https://git-scm.com/book/en/v2/Git-Tools-Submodules`, Japanese: `https://git-scm.com/book/ja/v2/Git-のさまざまなツール-サブモジュール`); GitHub documents scanning the entire Git history for committed credentials (`https://docs.github.com/en/code-security/secret-scanning/introduction/about-secret-scanning`).

---

### Task 1: Executable self-contained contract

**Files:**
- Create: `test/oss-self-contained.test.mjs`
- Create: `scripts/verify-oss-self-contained.mjs`
- Modify: `package.json`
- Modify: `.github/workflows/sec-scan.yml`

**Interfaces:**
- Consumes: Git index modes, `docs/manifests/oss-merge-1-sources.json`, repository root.
- Produces: `verifyRepository(root): { ok: boolean, violations: Array<{code:string,path:string,detail:string}> }`; CLI exits 0 only with zero violations.

- [x] **Step 1: Write the failing test**

  Add literal fixtures and real-repository assertions that catch:
  `160000` Gitlinks; symlinks resolving outside the checkout; missing canonical agent runner; active files containing forbidden local source roots; untracked source provenance; generated/runtime artifacts in active product paths; unsafe installer mutation in verification mode.

- [x] **Step 2: Run test to verify RED**

  Run: `node --test test/oss-self-contained.test.mjs`

  Expected: FAIL listing the two Gitlinks, missing `runtime/agent-runner/agent_runner.py`, and external runner reference from `run_agent.sh`/Rockstar_ibot consumers.

- [x] **Step 3: Implement the verifier**

  Implement the following closed rule set:

  ```js
  export const ACTIVE_ROOTS = [
    "apps/rockstar_ibot", "runtime", "scripts", "services", "skills",
    "install.sh", "start-local.sh"
  ];
  export const FORBIDDEN_SOURCE_ROOTS = [
    "/Users/operator", "~/profitable-claude", "$HOME/profitable-claude",
    "~/.openclaw", "$HOME/.openclaw"
  ];
  ```

  Git index parsing must use `git ls-files -s -z`, not filesystem traversal, so ignored runtime data cannot affect the result. Output contains paths and violation codes but never matching secret values.

- [x] **Step 4: Wire the verifier**

  Add root scripts:

  ```json
  {
    "scripts": {
      "test:oss": "node --test test/oss-self-contained.test.mjs",
      "verify:oss": "node scripts/verify-oss-self-contained.mjs"
    }
  }
  ```

  Add `npm run verify:oss` to the security workflow before language-specific jobs.

- [x] **Step 5: Run focused test**

  Run: `npm run test:oss`

  Expected: RED only for real repository violations; fixture unit cases PASS.

- [x] **Step 6: Commit**

  Commit: `test: define OSS self-contained contract`

### Task 2: Source manifest and reusable agent runner

**Files:**
- Create: `docs/manifests/oss-merge-1-sources.json`
- Create: `runtime/agent-runner/agent_runner.py`
- Create: `runtime/agent-runner/context_packet.py`
- Create: `runtime/agent-runner/token_budget.py`
- Create: `runtime/agent-runner/config.json`
- Create: `runtime/agent-runner/tests/`
- Modify: `skills/earn/marketing-engine/run_agent.sh`
- Modify: active consumer scripts under `skills/` and `apps/rockstar_ibot/scripts/`

**Interfaces:**
- Consumes: pinned committed tree `Daisuke134/profitable-claude@7f1a32897978b74013a9f09982f6179b9cc0d230:skills/agent-runner`.
- Produces: the single repository-local `runtime/agent-runner/agent_runner.py`; runtime config resolves from `AGENT_RUNNER_CONFIG` or repository-local `config.json`, with user overrides outside the repo.

- [x] **Step 1: Record provenance before copying**

  Manifest entries contain source repository, commit, source path, target path, license, disposition (`absorbed`, `external-state`, `not-required`), and SHA-256 per absorbed file. Record `anicca-products`, `profitable-claude`, `anicca-dais`, and the canonical Rockstar_ibot feature branches without local absolute paths.

- [x] **Step 2: Import only committed reusable files**

  Import the pinned upstream `skills/agent-runner` tree mechanically into canonical `runtime/agent-runner`, excluding private config values. Preserve its existing unit tests and keep one shared config.

- [x] **Step 3: Generalize configuration**

  Remove personal email, personal workspace, and account names. Use `$XDG_STATE_HOME`/`$HOME/.local/state/rockstar_ibot` defaults and environment overrides. No provider credential is required to import or run deterministic tests.

- [x] **Step 4: Route consumers locally**

  `run_agent.sh` resolves:

  ```bash
  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  REPO_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"
  RUNNER="${AGENT_RUNNER_BIN:-$REPO_ROOT/runtime/agent-runner/agent_runner.py}"
  ```

  Rockstar_ibot and marketing consumers resolve the shared runner relative to their repository location or an explicit environment override.

- [x] **Step 5: Run RED/GREEN tests**

  Run:

  ```bash
  python3 -m unittest discover -s runtime/agent-runner/tests -p 'test_*.py'
  python3 skills/earn/marketing-engine/test_gpt_first_runner_wiring.py
  python3 skills/earn/marketing-engine/test_capafy_loop_wiring.py
  node --test test/oss-self-contained.test.mjs
  ```

- [x] **Step 6: Commit**

  Commit: `feat: absorb shared agent runner`

### Task 3: Consolidate active marketing and browser branches

**Files:**
- Modify: files changed by Rockstar_ibot commits `58ccec55b`, `368dafb7d`, `23154626b`, `6a19897c4`, `1e1183168`
- Modify: files changed by browser branch `0f6870df44fe160b83aec58803a63c2d7b5e21d7`
- Modify: `apps/rockstar_ibot/lib/stagehand-steel-driver.js`
- Modify: `apps/rockstar_ibot/lib/stagehand-steel-driver.test.js`
- Modify: `docs/manifests/oss-merge-1-sources.json`

**Interfaces:**
- Consumes: source commits named above and follow-up `94485827497d6f0194f82f8f0c4f334427fa66d5`.
- Produces: canonical marketing runner wiring and latest browser-auth code; no Rockstar_ibot implementation remains only on legacy branches.

- [x] **Step 1: Apply the five marketing commits**

  Cherry-pick in chronological order and resolve only against canonical paths. Do not import runtime logs/state from the dirty local checkout.

- [x] **Step 2: Run marketing tests**

  Run the two marketing wiring tests and all modified loop tests. Expected: PASS with repository-local runner.

- [x] **Step 3: Apply browser-auth branch and follow-up**

  Cherry-pick implementation/test files from `0f6870df`, keep the current canonical master spec when resolving the documentation conflict, then cherry-pick `944858274`.

- [x] **Step 4: Run browser-auth tests**

  Run:

  ```bash
  node --test apps/rockstar_ibot/lib/browser-auth-session-store.test.js \
    apps/rockstar_ibot/lib/browser-job-store.test.js \
    apps/rockstar_ibot/lib/stagehand-steel-driver.test.js
  ```

- [x] **Step 5: Commit**

  Commit: `feat: consolidate active Rockstar_ibot branches`

### Task 4: Remove Git submodules without recreating external local folders

**Files:**
- Delete: `.gitmodules`
- Remove Gitlink: `services/inbox-zero`
- Remove Gitlink: `services/facilitator/x402-rs`
- Create: `services/facilitator/fetch-x402-rs.sh`
- Create: `services/facilitator/THIRD_PARTY.md`
- Modify: `services/facilitator/start.sh`
- Modify: `services/facilitator/README.md`
- Modify: `services/README.md`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: public pinned upstream `x402-rs/x402-rs@d439a91bda1caee486b0f841c4c6dd265fbee9df`.
- Produces: checksum-verified source cache under `${XDG_CACHE_HOME:-$HOME/.cache}/rockstar_ibot/x402-rs/<commit>` and an executable path returned on stdout. Inbox Zero remains an optional documented integration and is not a Rockstar_ibot runtime dependency.

- [x] **Step 1: Write fetch-script tests**

  Test rejects a wrong SHA-256, reuses a verified cached tree, and never reads another local checkout.

- [x] **Step 2: Verify RED**

  Run the fetch-script tests before implementing the script. Expected: missing script failure.

- [x] **Step 3: Implement pinned fetch**

  Download the public GitHub commit archive, validate the literal SHA-256, extract into the Rockstar_ibot cache, retain upstream Apache-2.0 license, and build there. Do not execute unverified content.

- [x] **Step 4: Update facilitator startup**

  `start.sh` calls the fetcher only when no explicit `X402_RS_ROOT` is provided. Runtime state stays under `services/facilitator/state/` and remains ignored.

- [x] **Step 5: Remove Gitlinks and verify**

  Run `npm run verify:oss`; expected Gitlink count 0. Run existing facilitator port contract test.

- [x] **Step 6: Commit**

  Commit: `build: replace submodules with pinned dependency fetch`

### Task 5: Portable active paths and public tree hygiene

**Files:**
- Modify: active scripts/configs reported by `npm run verify:oss`
- Modify: `.gitignore`
- Remove: tracked runtime state/log/output/scratch/generated files
- Modify: PII-shaped fixtures to use reserved example data

**Interfaces:**
- Consumes: verifier violation list.
- Produces: active paths computed from script location, `LIFE_MANAGER_HOME`, `XDG_STATE_HOME`, or `$HOME`; no personal home/user identifiers.

- [x] **Step 1: Fix one violation class at a time**

  Use these portable rules:

  ```text
  source checkout: derive from script dirname
  runtime state:   ${LIFE_MANAGER_HOME:-${XDG_STATE_HOME:-$HOME/.local/state}/rockstar_ibot}
  logs:            $LIFE_MANAGER_HOME/logs
  generated work:  $LIFE_MANAGER_HOME/work
  ```

- [x] **Step 2: Replace fixed launchd files with templates**

  Checked-in plists contain `__REPO_ROOT__`, `__LIFE_MANAGER_HOME__`, and `__HOME__`; installers render them. No `/Users/...` literal remains in active files.

- [x] **Step 3: Remove generated/runtime copies**

  Remove tracked log, output, scratch image, credential, session, state, and generated evidence artifacts from the public tree. Keep source fixtures only when tests consume them and they contain reserved/example data.

- [x] **Step 4: Run security gates**

  Run the repository PII-shape scan, gitleaks working-tree scan, Python syntax/unit tests, and shell syntax tests. Expected: zero new or current-tree violations.

- [x] **Step 5: Run the OSS verifier**

  Run: `npm run verify:oss`

  Expected: zero violations before moving to installer verification.

- [x] **Step 6: Commit**

  Commit: `refactor: make Rockstar_ibot public tree portable`

  Evidence: `npm run verify:oss` PASS; tracked PII shape grep 0; `gitleaks dir` 0;
  shell and Python syntax PASS; app suite 669/669 and panel score 14/14; Gitlinks 0;
  Camofox pinned archive fetch/checksum/install PASS.

### Task 6: Safe installer and fresh-clone proof

**Files:**
- Modify: `install.sh`
- Create: `scripts/verify-fresh-clone.sh`
- Modify: `README.md`
- Modify: `README.ja.md`
- Modify: `docs/superpowers/specs/2026-07-19-anicca-one-repo-consolidation-spec.md`
- Create: `docs/evidence/oss/oss-merge-1.md`

**Interfaces:**
- Consumes: clean Git branch commit and temporary directories.
- Produces: isolated clone verification report containing commit, commands, exit codes, test/eval counts, installer destination, and zero external-source paths.

- [x] **Step 1: Write installer integration RED**

  In a temporary HOME and `LIFE_MANAGER_INSTALL_DAEMON=0`, run `./install.sh`; assert runtime files are installed only under the provided `LIFE_MANAGER_HOME`, no LaunchAgent is written, and a second run preserves `.env`.

- [x] **Step 2: Implement safe installer mode**

  Check every required binary including `rsync`; add `LIFE_MANAGER_INSTALL_DAEMON=${LIFE_MANAGER_INSTALL_DAEMON:-1}` and skip host service registration when it is `0`. Install root and `apps/rockstar_ibot` dependencies using lockfiles without modifying them.

- [x] **Step 3: Implement fresh-clone verifier**

  Clone the current branch into `mktemp -d`, unset source-checkout variables, run:

  ```bash
  npm ci
  npm run verify:oss
  LIFE_MANAGER_HOME="$tmp/runtime" LIFE_MANAGER_INSTALL_DAEMON=0 ./install.sh
  (cd apps/rockstar_ibot && npm ci && npm test && npm run eval && npm run eval:panel-privacy)
  ```

  Compare `git status --porcelain` before and after; expected clean.

- [x] **Step 4: Run full verification**

  Run focused tests, root security gates, `apps/rockstar_ibot` full tests/evals, and `scripts/verify-fresh-clone.sh`.

- [x] **Step 5: Update state**

  Mark `OSS-MERGE-1` done only with the clean-clone commit/evidence. Move the product cursor to `BROWSER-AUTH-1`; do not alter later TODO status.

- [x] **Step 6: Commit**

  Commit: `docs: close OSS-MERGE-1 with fresh-clone evidence`

  Evidence: remote depth-one clone at `1d3f579829e548d63b92a82ca07a673adb5a8970`
  remained clean; install isolation 1/1; app 669/669; panel 14/14; all eight deterministic
  eval groups and panel privacy PASS. See `docs/evidence/oss/oss-merge-1.md`.

## Self-Review

- Spec coverage: source/target manifest, history/license provenance, secret/PII/generated artifact boundary, private URL/local path/Gitlink/symlink escape/runtime copy, install/focused/full/eval/fresh clone are each assigned above.
- No placeholder steps remain; every task has exact files, commands, and exit conditions.
- Interface consistency: Tasks 2–5 all feed the `verifyRepository()` boundary from Task 1; Task 6 consumes the same CLI in a new clone.
- Scope remains one atomic: all tasks are required to make `OSS-MERGE-1` true and none advances browser execution beyond consolidating the already-existing branch.

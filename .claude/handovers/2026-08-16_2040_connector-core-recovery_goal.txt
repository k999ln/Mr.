/goal Restore the local Rockstar_ibot Connector core until the seven items in `docs/superpowers/specs/2026-08-01-dais-rockstar_ibot-five-phase-execution-spec.md` section `0.2.1 Active remaining TODO SSOT` are evidence-backed DONE, reading `.claude/handovers/2026-08-16_2040_connector-core-recovery.md` and spec sections `0.1.1`–`0.2.3` first and treating them as scope authority. The user-visible outcome is: Mac mini runs the official `skills/connector/run.sh`; Luma then Connpass are primary and existing other providers remain fallback; AI/crypto is a soft preference that never excludes unrelated eligible events; a verified registration writes exactly the event itself to Google Calendar and sends Telegram event/Calendar links plus verified registration evidence and an official QR only when the provider exposes one. Connector must never calculate or write travel time, buffers, routes, or Rockstar_ibot Web App enrichment. Worktree routing: fetch `/Users/operator/Projects/rockstar_ibot-main`; do not modify its shared `main` checkout, `.worktrees/connector-core-handover-20260816`, or any healer worktree. From current `origin/main` containing verified commit `f32eee4d2392a63597b3505a49b2bf50fdb1145d`, create `/Users/operator/Projects/rockstar_ibot-main/.worktrees/connector-core-recovery` on branch `fix/connector-core-recovery`, upstream/push target `origin/fix/connector-core-recovery`; production, tests, spec, and execution notes are written only there. Reviewers use a temporary detached snapshot of the exact reviewed commit. Before editing, report fetched HEAD/upstream/dirty state and repeat read-only launchd/plist/`:9222`/`:9223`/`:18793`/process/lock/latest-wake/bundle checks. Complete C-CORE-01→07 serially: sole native owner; dedicated `:9222` readiness without touching Gig `:9223`; remove native `routeMinutes`/`homeLocation` travel gating and prove no route API or travel Calendar write; one supervised primary-first native wake; current-runtime Luma and Connpass end-to-end evidence when real non-conflicting candidates exist; then natural 09:00 or login-recovery proof. Never stop/kill shared browser or OS foundation services, delete plists/state, retry an unknown external effect, create duplicate applications/Calendar events, weaken tests, or broaden fallback/cloud scope. Use the smallest TDD slice per item, update SSOT and concise execution notes after every evidence pass, fetch/commit/push each meaningful change, and continue until Done rather than stopping at a plan. For Codex, use `spawn_agent`: Sol owns spec/acceptance/E2E, Luna implements assigned production/tests only, and a fresh read-only Sol reviewer adversarially checks each risky slice; in Claude Code use equivalent isolated implementer and fresh read-only reviewer. Done requires fresh Connector tests, diff checks, exact launchd/port/process cleanup, official provider readback, exact-one Calendar readback, positive Telegram IDs, durable receipt lineage, and PR/CI/merge evidence; fixtures/dry runs are supporting evidence only. If a real Luma/Connpass candidate is absent, finish every unblocked item, record that item `BLOCKED_EXTERNAL` with counts, durable owner, restart condition, and next scheduled check; stop only after three distinct safe approaches repeat the same blocker or a required credential/production approval is unavailable, and report the smallest next action without claiming Done.

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Capafy loop pass",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "evidence"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "no_op", "failure"],
      "description": "failure means any required action, report, or readback failed; success means required work and official effect are complete; no_op means a legitimate external no-write terminal."
    },
    "evidence": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string"}
    }
  }
}
